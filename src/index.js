import _ from 'lodash';
import "./style.scss"
import { runDisplay } from './display';



runDisplay();

//the project will have the following structure:


//the display generator,
//the calculations and operations part
//the index, that will call the display, 
//from the display the other functions will be generated and called
